Vim
===
My vimrc etc.

Installation
------------
    git clone --recursive git://github.com/jbro/vim.git ~/.vim && \
    sh ~/.vim/update && \
    ln -s ~/.vim/vimrc ~/.vimrc

Update
------
    sh ~/.vim/update.sh
